enum strPatt {
    SPECIAL_CHARACTER = 'special',
    NUMBER = 'number',
    CAPITAL = 'capital',
}

enum imageExt {
    JPEG = '.JPEG',
    JFIF = '.JFIF',
    JPG = '.JPG',
    GIF = '.GIF',
    PNG = '.PNG',
}

enum fileUnit {
    BYTE = 'byte',
    KB = 'kb',
    MB = 'mb',
    GB = 'gb',
    TB = 'tb',
}

enum eNumbers {
    RANDOM_NUM = 'randomNum',
}

export {strPatt, eNumbers, imageExt, fileUnit};
